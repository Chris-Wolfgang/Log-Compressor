# Hash-pinned so `pip install --require-hashes` can verify the download (OSSF
# Scorecard Pinned-Dependencies: "pipCommand not pinned by hash"), matching
# semgrep.txt and zizmor.txt. The two hashes are defusedxml 0.7.1's published
# sha256 digests for the wheel and the sdist, so the pin holds whichever pip
# resolves to.
#
# Regenerate with:
#    uv pip compile --universal --generate-hashes defusedxml.in -o defusedxml.txt
defusedxml==0.7.1 \
    --hash=sha256:1bb3032db185915b62d7c6209c5a8792be6a32ab2fedacc84e01b52c51aa3e69 \
    --hash=sha256:a352e7e428770286cc899e2542b6cdaedb2b4953ff269a210103ec58f6198a61
